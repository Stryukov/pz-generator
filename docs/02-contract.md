# Контракт на результат

> Статус: в разработке (следующий шаг).

Общие правила генерации, схема JSON-манифеста, правила валидации (арифметическая
сверка с расчётом), ограничения (verbatim-болванки, обработка «неровного» месяца).
