# pz-generator

Автоматическая генерация пояснительных записок к расчётам задолженности за услуги
водоснабжения и водоотведения. Пайплайн: расчёт из биллинга + доп-контекст исполнителя →
ИИ по спецификации → готовый DOCX. Оркестрация — n8n.

## Статус
В разработке. Согласована спецификация для документов **Типа А** (коммерческий абонент с
платой за негативное воздействие, НВЦС). Тип Б (формальные записки с иными НПА) — отложен.

## Что в репозитории

```
pz-generator/
├── README.md                              — этот файл
├── docs/
│   ├── 00-flow-n8n.md                      — оркестрация пайплайна (обзор)
│   ├── 01-extraction-contract.md           — [извлечение] контракт агента
│   ├── 02-manifest-schema.md               — [общий] схема манифеста
│   ├── 03-validation-rules.md              — [валидация] правила + референс-код
│   ├── 04-spec-type-A.md                   — [генерация] структура записки
│   └── 05-generation-contract.md           — [генерация] контракт агента
├── templates/
│   ├── document-template-type-A.json       — болванки + правила сборки (справочно)
│   └── executor-context.md                 — мини-шаблон доп-контекста
├── n8n/
│   ├── pz-generator.workflow.json            — импортируемый флоу
│   └── README.md                           — импорт и настройка
└── CHANGELOG.md
```

## Артефакты по этапам пайплайна

| Этап | Читает | Вход-данные |
|---|---|---|
| **Извлечение** (шаг 2) | `docs/01-extraction-contract.md`, `docs/02-manifest-schema.md` | расчёт + доп-контекст |
| **Валидация** (шаг 3) | `docs/03-validation-rules.md` (код) | манифест |
| **Генерация** (шаг 4) | `docs/04-spec-type-A.md`, `templates/document-template-type-A.json`, `docs/05-generation-contract.md` | манифест |

Общий стык всех этапов — **манифест** (`docs/02-manifest-schema.md`): производится извлечением,
потребляется валидацией и генерацией.

## Архитектура обработки (3 шага)
1. **Извлечение** — ИИ читает расчёт → структурированный JSON-манифест (флаги + числа).
2. **Валидация** — код сверяет арифметику манифеста с расчётом.
3. **Генерация** — ИИ по манифесту и спецификации собирает прозу записки (DOCX).

## Глоссарий
- **ПЗ** — пояснительная записка.
- **НВЦС** — плата за негативное воздействие на централизованную систему водоотведения
  (п. 123(4) Правил № 644).
- **п. 203** — плата за сброс загрязняющих веществ сверх нормативов (самостоятельное
  начисление, не часть НВЦС).
- **ХВС / ВО** — холодное водоснабжение / водоотведение.
- **ПУ** — прибор учёта.
- **Правила № 644** — ПП РФ от 29.07.2013 № 644.

## Конвенции
- Версионирование — через историю git; суффиксы `_v2` в именах файлов не используются.
- Плейсхолдеры в записке — угловые скобки `<…>`, оформленные цветным кодом (`<code class="ph">`).
- Юридические болванки вставляются **дословно**, ИИ их не переписывает.
